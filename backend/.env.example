# Database connection URL for PostgreSQL (e.g. postgresql://user:password@localhost:5432/aeternum?schema=public)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aeternum?schema=public"

# JWT configuration secret (used to sign session tokens)
JWT_SECRET="aeternum_fallback_secret_key_change_me_in_production"
